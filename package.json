{
  "name": "automerge-encryption-demo",
  "version": "1.0.0",
  "description": "This repo is a proof of concept for using Automerge with client-side encryption.",
  "main": "index.js",
  "scripts": {
    "install": "concurrently 'npm:install:*'",
    "install:server": "cd server && npm install",
    "install:client": "cd client && npm install",
    "start": "concurrently 'npm:start:*'",
    "start:server": "cd server && npm start",
    "start:client": "cd client && npm start"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^7.4.0"
  }
}
